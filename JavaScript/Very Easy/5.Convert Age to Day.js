// Create a function that takes the age in years and returns the age in days.
// Use 365 days as the length of a year for this challenge.
// Ignore leap years and days between last birthday and now.
// Expect only positive integer inputs.
// Examples
// calcAge(65) ➞ 23725
// calcAge(0) ➞ 0
// calcAge(20) ➞ 7300
// Don't forget return 

function calculateAge(age){
    
}

calculateAge(65)===23725? console.log('Test Passed : Value==',calculateAge(65)):
console.log('Test Passed : Value==',calculateAge(65));
calculateAge(0)===0? console.log('Test Passed : Value==',calculateAge(0)):
console.log('Test Passed : Value==',calculateAge(0));
calculateAge(20)===7300? console.log('Test Passed : Value==',calculateAge(20)):
console.log('Test Passed : Value==',calculateAge(20));
